import Theme from 'models/Theme'

const colors = {
	replaceMe: '#123456',
}

const dragonFireTheme: Theme = {
	foo: colors.replaceMe,
	bar: colors.replaceMe,
	baz: colors.replaceMe,
}

export default dragonFireTheme
